# ASK

It is a project modeled on ask.fm. In short you can ask questions any user and waiting for answered. 
When I created this project I focused on relations, tests, signals, random question. Project is not finished.

## Setup

The `requirements.txt` have list all Python libraries. They will be installed using:

`pip install -r requirements.txt`